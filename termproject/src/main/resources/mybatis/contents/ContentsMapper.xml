<?xml version="1.0" encoding="UTF-8"?>
  <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.tukorea.common.contents.dao.ContentsMapper">
<select id = "selectContentsListTotalCount" parameterType = "java.util.HashMap" resultType = "Integer">
	SELECT
		COUNT(*) AS TOTAL_CNT
	FROM
		CONTENTS
</select>
<select id = "selectContentsList" resultType = "com.tukorea.common.contents.domain.Contents">
	SELECT
		CONTENTS_ID,
		TITLE,
		GENRE,
		YEAR,
		DIRECTOR,
		RUNNING,
		SUMMARY,
		HITS,
		KIND,
		POSTER_IMG_NAME,
		POSTER_IMG_SAVE_PATH
	FROM
		CONTENTS
	WHERE
		KIND='MOVIE'
	ORDER BY
		CONTENTS_ID DESC
</select>
<!-- 게시판 상세 조회 -->
	<select id="selectContentsInfo" parameterType="Integer" resultType="com.tukorea.common.contents.domain.Contents">
		SELECT
			CONTENTS_ID,
			TITLE,
			GENRE,
			YEAR,
			DIRECTOR,
			RUNNING,
			SUMMARY,
			HITS,
			KIND,
			DATE_FORMAT(REG_DT, '%Y-%m-%d %H:%i') AS REG_DT,
			POSTER_IMG_SAVE_PATH
		FROM
			CONTENTS
		WHERE
			CONTENTS_ID = #{contents_id}
	</select>
	<update id="updateContentsHits" parameterType="Integer">
		UPDATE CONTENTS
		SET
			HITS = HITS + 1
		WHERE
			CONTENTS_ID = #{contents_id}
	</update>
	<insert id="insertContents" parameterType="java.util.HashMap">
		INSERT INTO CONTENTS (
			TITLE,
			GENRE,
			YEAR,
			DIRECTOR,
			RUNNING,
			SUMMARY,
			HITS,
			KIND,
			POSTER_IMG_NAME,
			POSTER_IMG_SAVE_PATH,
			REG_DT
		) VALUES (
			#{title},
			#{genre},
			#{year},
			#{director},
			#{running},
			#{summary},
			#{hits},
			#{kind},
			#{imageName},
			#{savePath},
			NOW()
		)
		<selectKey keyProperty="contents_id" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	<delete id="deleteBoard" parameterType="Integer">
		DELETE FROM CONTENTS
		WHERE
			CONTENTS_ID = #{contents_id}
	</delete>
	<!-- 게시물 글 소유자 확인 -->
	<select id="selectContentsPasswordForCheck" parameterType="java.util.HashMap" resultType="Integer">
		SELECT
			COUNT(*) AS TOTAL_CNT
		FROM
			CUSTOMER
		WHERE
			TYPE = 'ADMIN'
			AND PASSWORD = SHA2(#{password}, 256)
	</select>

</mapper>